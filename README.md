Commands to run install all requirements : 



Command to run to create venv : `python3 -m venv ./venv`
And to activate it : `source ./venv/bin/activate`

To update the requirements.txt : `pip3 freeze > requirements.txt`

To install requirements `pip3 install -r requirements.txt`